<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    style="@style/myListItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/blue_outline_white_background"
    android:layout_marginTop="@dimen/margin_list_item_vert"
    android:layout_marginBottom="@dimen/margin_list_item_vert"
    android:layout_marginStart="@dimen/margin_outside"
    android:layout_marginEnd="@dimen/margin_outside"
    android:elevation="2dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:orientation="vertical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/list_item_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/granite_dark"
            android:textSize="18sp"
            android:textStyle="bold"
            tool:hint="@string/hint_series_title" />

        <TextView
            android:id="@+id/list_item_dates"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tool:hint="(1990-2005)"
            android:textSize="16sp"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>